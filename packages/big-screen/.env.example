# Point at your local server while developing:
VITE_SERVER_URL=http://localhost:4000
# Once phone-client is deployed, put its real URL here so the lobby QR code works:
VITE_PHONE_CLIENT_URL=http://localhost:5174
